# Final Submission Files

## RTL

- `core.v`

This is copied from `core_2line_bypass_fifo_pixpipe_lanepix_drain11.v`, the final selected RTL version.

## Constraint

- `core_v_original.sdc`

This SDC is separated from the historical constraints and is the final constraint file used for the selected result.

Before submitting or running the course scripts, copy `final_submission/core.v` into the expected RTL run location if the script requires the file to be named exactly `core.v`.
